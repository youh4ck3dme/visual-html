{
  "name": "pngtohtmlapp",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix visual-html",
    "build": "NITRO_PRESET=vercel npm run build --prefix visual-html",
    "test": "npm run test --prefix visual-html",
    "lint": "npm run lint --prefix visual-html",
    "typecheck": "npm run typecheck --prefix visual-html",
    "format": "npm run format --prefix visual-html",
    "test:integrity": "npm run test:integrity --prefix visual-html",
    "test:integrity:fast": "npm run test:integrity:fast --prefix visual-html",
    "preview": "npm run preview --prefix visual-html",
    "github:gate": "bash scripts/github-release-gate.sh",
    "github:gate:full": "bash scripts/github-release-gate.sh --full"
  }
}