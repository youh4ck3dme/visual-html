Pixel-perfect vernosť
Recreate this screenshot as close as possible to the original. Match spacing, typography scale, border radius, shadows, and layout proportions. Use semantic HTML and clean, maintainable CSS variables.

Modern landing page
Rebuild this as a modern, production-ready landing page with clear hierarchy, strong hero section, CTA button, responsive cards, and polished spacing rhythm. Keep code clean and accessible.

Tailwind štýl
Generate using Tailwind utility classes only. Do not include CDN links. Keep markup semantic, mobile-first, and easy to extend.

Komponentový výstup
Return output as reusable component-like structure with clear section wrappers and meaningful class names. Prioritize readability and future edits.

Accessibility strict
Use strict accessibility: proper heading order, aria-labels on controls, alt text for images, visible focus states, high contrast, and keyboard-friendly interactions.

Mobile-first
Design mobile-first for 360px first, then scale to 768, 1024, and 1440 breakpoints. Preserve visual consistency across breakpoints.

Minimal clean code
Avoid unnecessary wrappers and div nesting. Keep HTML concise, CSS organized by sections, and JavaScript empty unless interaction is clearly needed.

Dark theme variant
Recreate this in a dark professional theme with neutral palette, readable contrast, subtle shadows, and consistent spacing.

SaaS dashboard look
Convert into a SaaS dashboard style: top nav, sidebar, metric cards, tables/charts placeholders, and clean enterprise visual language.

UI polish + microinteractions
Add subtle hover/focus transitions and polished states for buttons, cards, and inputs. Keep interactions lightweight and non-distracting.

Odporúčanie:
Najlepší výsledok býva kombinácia:

štýl (napr. modern landing page),
technické pravidlá (semantic HTML, no CDN),
responzivita (mobile-first breakpoints),
accessibility požiadavky.
Ak chceš, pripravím ti 5 krátkych “best practice” promptov presne pre tvoj typ obrázkov (landing page, dashboard, auth screen, pricing page, admin panel).
